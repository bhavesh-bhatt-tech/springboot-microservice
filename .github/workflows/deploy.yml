name: Build and Publish Docker Images
on:
  push:
    branches:
      - main
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      with: { image: bkbdocker/gateway-service, tags: latest, registry: docker.io, dockerfile: Dockerfile, enableBuildKit: true, username: "${{ secrets.DOCKER_USERNAME }}", password: "${{ secrets.DOCKER_PASSWORD }}" }
    uses:

